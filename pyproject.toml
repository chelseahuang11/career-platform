[project]
name = "career-platform"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "chelseahuang11", email = "chelseahuang11@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "jinja2>=3.1.6",
    "pytest>=9.1.1",
    "sqlite-utils>=4.2.1",
    "uvicorn>=0.53.0",
]

[project.scripts]
career-platform = "career_platform:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
